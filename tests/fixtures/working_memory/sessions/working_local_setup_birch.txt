User: I am still setting up birch-service locally.
Assistant: You already pulled the repo, but you still need BIRCH_API_KEY in .env.local before docker compose up birch-service works.
User: Next add BIRCH_API_KEY to .env.local and rerun docker compose up birch-service.
