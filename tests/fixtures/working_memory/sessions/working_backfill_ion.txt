User: I reran python3 scripts/backfill.py --job ion_rebuild in staging.
Assistant: Counts matched after the rerun, but I still need one more dashboard check before widening the rollout.
User: Next watch the error-rate dashboard for 20 minutes and then decide whether to widen the rollout.
