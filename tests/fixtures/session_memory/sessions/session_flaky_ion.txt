User: I was triaging the flaky pytest job for ion-service.
Assistant: PYTEST_ADDOPTS=-x and pytest tests/integration/test_ion.py -k refresh_flow reproduced it faster.
User: We did not fix it yet; next session should capture the failing seed and isolate the race.
