User: Can we time how much time each prompt embedding takes, and how time we take to search the memory before returning the response. Same with extraction for each session by our dear daemon. Show a graph for each in dashboard, so we can adjust the timeout. Should be in overview landing page. Also the overview shows this at the bottom of the page - "The Process One Session button asks the daemon pipeline to pick the next unprocessed session and run one extraction pass immediately. Savings metrics are approximate and come from direct memory-answer hits logged in ~/.memory/activity.log using a rough chars / 4 heuristic. Performance charts also read from ~/.memory/activity.log and show recent prompt-embedding, memory-search, and daemon session-extraction durations to help tune timeouts." This should be tooltip for the button "Process One Session".
