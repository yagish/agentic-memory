{
  "title_contains": [
    "auth-api",
    "backfill"
  ],
  "summary_contains": [
    "auth-api",
    "safe backfill"
  ],
  "steps_contains": [
    "pause the cron",
    "run python3 scripts/backfill.py --job auth_api_backfill --limit 500",
    "verify the row count in staging",
    "resume the cron",
    "watch the error rate dashboard"
  ],
  "trigger_phrases_contains": [
    "run backfill for auth-api",
    "backfill"
  ],
  "tools_contains": [
    "cron",
    "scripts/backfill.py",
    "auth_api_backfill",
    "staging",
    "AUTH_DB_URL"
  ]
}
