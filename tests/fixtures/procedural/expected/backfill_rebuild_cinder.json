{
  "title_contains": [
    "billing-api",
    "backfill"
  ],
  "summary_contains": [
    "billing-api",
    "safe backfill"
  ],
  "steps_contains": [
    "pause the cron",
    "run python3 scripts/backfill.py --job billing_api_backfill --limit 500",
    "verify the row count in staging",
    "resume the cron",
    "watch the error rate dashboard"
  ],
  "trigger_phrases_contains": [
    "run backfill for billing-api",
    "backfill"
  ],
  "tools_contains": [
    "cron",
    "scripts/backfill.py",
    "billing_api_backfill",
    "staging",
    "BILLING_DB_URL"
  ]
}
