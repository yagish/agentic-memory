User: When the worker-service feature flag misbehaves, first disable WORKER_SERVICE_FLAG, then clear the edge cache, then retry with an employee account.
Assistant: If it still fails, capture the request id and page support.
User: Mention WORKER_QUEUE_URL in the incident notes if config drift caused it.
