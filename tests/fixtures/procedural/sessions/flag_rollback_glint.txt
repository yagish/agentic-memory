User: When the export-api feature flag misbehaves, first disable EXPORT_API_FLAG, then clear the edge cache, then retry with an employee account.
Assistant: If it still fails, capture the request id and page support.
User: Mention EXPORT_BUCKET in the incident notes if config drift caused it.
