User: The pricing-api deploy workflow is always build the Docker image, run alembic upgrade, roll the pricing-api deployment in staging, verify /healthz, then promote production.
Assistant: I'll preserve those exact steps.
User: If PRICING_DB_URL is missing, stop before promotion.
