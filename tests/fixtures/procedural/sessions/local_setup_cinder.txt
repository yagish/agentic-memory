User: The local setup for billing-api is clone the repo, copy .env.example to .env.local, export BILLING_DB_URL, run docker compose up billing-api, then run pytest tests/billing_api.
Assistant: That sounds repeatable.
User: If migrations are pending, run alembic upgrade before opening the app.
