User: The local setup for auth-api is clone the repo, copy .env.example to .env.local, export AUTH_DB_URL, run docker compose up auth-api, then run pytest tests/auth_api.
Assistant: That sounds repeatable.
User: If migrations are pending, run alembic upgrade before opening the app.
