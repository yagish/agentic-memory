User: When the admin-web feature flag misbehaves, first disable ADMIN_WEB_FLAG, then clear the edge cache, then retry with an employee account.
Assistant: If it still fails, capture the request id and page support.
User: Mention ADMIN_BASE_URL in the incident notes if config drift caused it.
