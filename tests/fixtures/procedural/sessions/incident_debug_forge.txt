User: The incident debug workflow for worker-service is collect the request id, check the worker-service-latency dashboard, tail the api logs, compare WORKER_QUEUE_URL, then roll back if the mismatch explains the failure.
Assistant: If rollback does not help, page the on-call and capture the failing payload.
