User: When the billing-api feature flag misbehaves, first disable BILLING_API_FLAG, then clear the edge cache, then retry with an employee account.
Assistant: If it still fails, capture the request id and page support.
User: Mention BILLING_DB_URL in the incident notes if config drift caused it.
