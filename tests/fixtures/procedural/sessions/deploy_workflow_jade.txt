User: The mobile-backend deploy workflow is always build the Docker image, run alembic upgrade, roll the mobile-backend deployment in staging, verify /healthz, then promote production.
Assistant: I'll preserve those exact steps.
User: If MOBILE_API_URL is missing, stop before promotion.
