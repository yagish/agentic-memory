User: The local setup for export-api is clone the repo, copy .env.example to .env.local, export EXPORT_BUCKET, run docker compose up export-api, then run pytest tests/export_api.
Assistant: That sounds repeatable.
User: If migrations are pending, run alembic upgrade before opening the app.
