User: The incident debug workflow for export-api is collect the request id, check the export-api-latency dashboard, tail the api logs, compare EXPORT_BUCKET, then roll back if the mismatch explains the failure.
Assistant: If rollback does not help, page the on-call and capture the failing payload.
