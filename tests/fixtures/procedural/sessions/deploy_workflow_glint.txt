User: The export-api deploy workflow is always build the Docker image, run alembic upgrade, roll the export-api deployment in staging, verify /healthz, then promote production.
Assistant: I'll preserve those exact steps.
User: If EXPORT_BUCKET is missing, stop before promotion.
