User: The incident debug workflow for admin-web is collect the request id, check the admin-web-latency dashboard, tail the api logs, compare ADMIN_BASE_URL, then roll back if the mismatch explains the failure.
Assistant: If rollback does not help, page the on-call and capture the failing payload.
