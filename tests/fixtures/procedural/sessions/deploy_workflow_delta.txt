User: The docs-site deploy workflow is always build the Docker image, run alembic upgrade, roll the docs-site deployment in preview, verify /healthz, then promote production.
Assistant: I'll preserve those exact steps.
User: If DOCS_PREVIEW_URL is missing, stop before promotion.
