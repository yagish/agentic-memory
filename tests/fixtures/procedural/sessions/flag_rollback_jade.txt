User: When the mobile-backend feature flag misbehaves, first disable MOBILE_BACKEND_FLAG, then clear the edge cache, then retry with an employee account.
Assistant: If it still fails, capture the request id and page support.
User: Mention MOBILE_API_URL in the incident notes if config drift caused it.
