User: The search-api deploy workflow is always build the Docker image, run alembic upgrade, roll the search-api deployment in staging, verify /healthz, then promote production.
Assistant: I'll preserve those exact steps.
User: If SEARCH_INDEX_URL is missing, stop before promotion.
