User: The admin-web deploy workflow is always build the Docker image, run alembic upgrade, roll the admin-web deployment in staging, verify /healthz, then promote production.
Assistant: I'll preserve those exact steps.
User: If ADMIN_BASE_URL is missing, stop before promotion.
