User: The local setup for search-api is clone the repo, copy .env.example to .env.local, export SEARCH_INDEX_URL, run docker compose up search-api, then run pytest tests/search_api.
Assistant: That sounds repeatable.
User: If migrations are pending, run alembic upgrade before opening the app.
